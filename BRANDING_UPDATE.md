# İsim Değişikliği ve Repo Konumlandırması

**Yeni Repo Adı:** `visgate-deploy-api`
**Organizasyon/Kullanıcı:** `visgate-ai` (Önerilen)

**README.md Başlığı:** `Visgate Deploy API` olarak güncellendi.

Bu isim (`visgate-ai/visgate-deploy-api`), projenin amacını (deploy altyapısı sağlamak) ve kimliğini (Visgate markası) çok net anlatıyor.

**Sıradaki Adım:**
1.  GitHub üzerinde `visgate-ai` organizasyonunda (yoksa oluşturup) `visgate-deploy-api` adında **boş** bir repo açın.
2.  Mevcut kodu bu repoya pushlayın:
    ```bash
    git remote set-url origin https://github.com/visgate-ai/visgate-deploy-api.git
    git branch -M main
    git push -u origin main
    ```
